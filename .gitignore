# Never commit secrets, keys, loot, or engagement data
*.key
*.pem
id_*
id_*.pub
*.kdbx
.env
.env.*
# direnv per-engagement env (created by `denv`); never commit
.envrc
creds*
loot/
ops/
*.log

# OS / editor cruft
.DS_Store
*.swp
*~
